services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: subscription_allocator
      POSTGRES_USER: subscription_allocator
      POSTGRES_PASSWORD: subscription_allocator
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: